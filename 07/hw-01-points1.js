;'use strict';

// 2. Попадание точки в область

// 2.1


let x = +prompt('Input x', '');
let y = +prompt('Input y', '');

if (y > 0 && (y - (3 * x / 4) <= 3) && (y + (3 * x / 4) <= 3)) {
    alert('Точка попадает в область!');
} else {
    alert('Точка НЕ попадает в область');
}

